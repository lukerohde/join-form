<% if notice.present? %>
	<p id="notice" class="alert alert-info text-center alert-dismissible fixed-to-top" > 
		<%= notice %>
	</p>
<% end %>

<div class="col-sm-8 col-sm-offset-2">
	<%= render 'form', subscription: @subscription %>
</div>