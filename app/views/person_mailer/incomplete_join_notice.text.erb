This notice was sent because the join form hadn't been completed within half an hour of starting.  They may have completed it since.

<% if @subscription.person.present? %>
	<% @subscription.person.attributes.each do |k,v| %>
		<% if v.present?  %>
		  <%= "    #{k}: #{v}" %>
		<% end %>
	<% end %>
<% end %>

<% @subscription.attributes.each do |k,v| %>
  <% if v.present? %>
	  <%= "#{k}: #{v}" %>
	<% end %> 
<% end %>