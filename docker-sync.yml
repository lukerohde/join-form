version: "2"

options:
  verbose: true
syncs:
  joinform-www-sync: # tip: add -sync and you keep consistent names as a convention
    src: './www'
    # sync_strategy: 'native_osx' # not needed, this is the default now
    # sync_excludes: ['ignored_folder', '.ignored_dot_folder']
  joinform-api-nuw-membership-sync: # tip: add -sync and you keep consistent names as a convention
    src: './api-nuw-membership'
    # sync_strategy: 'native_osx' # not needed, this is the default now
    # sync_excludes: ['ignored_folder', '.ignored_dot_folder']
